<!DOCTYPE html>
<html>
	<head>
		<title>Hello {{ name }}</title>
	</head>
	<body>
		<h1>Hello {{ name }}</h1>
		{% if is_admin %}
			<p>Welcome, Admin.</p>
		{% else %}
		    <p>You are a guest.</p>
		{% endif %}
		<ul>
			{% for item in items %}
				<li>{{ item }}</li>
			{% endfor %}
		</ul>
	</body>
</html>	